{
    "base_url": {
        "description": "图床网址",
        "type": "string",
        "hint": "填写图床网址，用于获取随机图片和上传图片，无需结尾斜杠",
        "default": "",
        "obvious_hint": true
    },
    "upload_admin_only": {
        "description": "上传功能仅管理员可用",
        "type": "bool",
        "hint": "是否仅允许管理员使用上传功能",
        "default": true
    },
    "auth_code": {
        "description": "上传认证码（可选）",
        "type": "string",
        "hint": "用于上传API的认证码（可选）",
        "default": ""
    },
    "show_upload_link": {
        "description": "上传成功时显示链接",
        "type": "bool",
        "hint": "上传成功时是否在消息中显示返回的图片链接",
        "default": true
    },
    "local_random_type": {
        "description": "媒体类型本地随机",
        "type": "bool",
        "hint": "若开启且请求包含图片和视频，则在本地随机选取其中一种类型后再请求图床，用于平衡图片和视频出现概率。",
        "default": false
    }
}